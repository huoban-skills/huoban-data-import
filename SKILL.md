---
name: huoban-data-import
description: "把 Excel/CSV 批量导入伙伴云已有普通表，并让数据正确关联系统现有数据。用户要往伙伴云导入数据、导入新增一批数据时触发，先走本 skill 的两阶段流程再执行导入：先定拆分与数据主键并让用户确认，再做三层评估、出《导入方案》活文档、确认结果回来复检后执行到收尾。"
---

# 伙伴云数据导入

- **目标**：源文件数据进入目标表，关联字段指向正确的现有数据，匹配不上的部分成待确认清单，找谁确认由用户定。
- **输入**：本地 Excel/CSV 绝对路径、目标工作区。
- **输出**：阶段一拆分表与匹配机制表；阶段二一份《导入方案》活文档，两章：评估结果（概览、待确认）、执行清单（导入总览、系统改动、补索引、执行次序）；另有待导入/疑似匹配/待处理 Excel、已导入数据。交付目录建在用户工作目录下，按用户视角分层：`〈项目〉-〈主题〉导入/` 根放导入方案 md，`待导入/` 放确认后可直接导的 Excel，`待确认/` 放要逐条裁决的 Excel，`源文件/` 放原始 Excel 副本、关联表快照等工作底稿。
- **边界**：导入方式固定 create 只新增；更新或覆盖已有数据转 huoban-table。本地解析只用于评估和生成清洗文件，写入只走 `hac table +import-file`，列映射以 prepare 返回的 `auto_matches` / `mapping_field_options` 为准。

## 依赖

| 依赖 | 用途 |
| -- | -- |
| `scripts/get_source_profile.py` | 源文件列清单、行数、空值率、按主键判重，重复明细导出标黄 Excel |
| `scripts/render_match_report.py` | 源主键与关联表导出比对，三档计数，生成待导入与疑似匹配 Excel |
| `scripts/resweep.py` | 导入后复扫：本批新建的关联表记录与系统其它记录按同一口径再比一遍 |
| `scripts/relink.py` | 按裁决把本批导入记录的关联字段逐条改指到别的记录，落记录文件 |
| `hac table list-tables / get-table / +relation-graph` | 工作区表清单、字段配置、关联指向 |
| `hac table +schema --mode write` | 目标表必填与写入契约 |
| `hac table list-items` | 分页拉关联表数据落本地 CSV 供比对（`+export` 是写风险命令，纯读链路不用） |
| `hac table +import-file` | 唯一写入通道，编排读 huoban-table 的 references/shortcuts/import-file.md |
| `references/keys-and-split.md` | 主键判定与拆分口径 |
| `references/assessment.md` | 三层评估逐项判定 |
| `references/execution.md` | 导入编排、基线、小样本、回读 |

脚本用本 skill 目录下的绝对路径调用，`_common.py` 与脚本同目录。

## 执行流程

```
摸现状 → 拆分表+匹配机制表 →【用户确认】→ 三层评估 → 导入方案首版待确认 → 确认后复检填定 → 系统改动/补索引/补关联数据 → 小样本 → 分批导入+回读 → 复扫本批新建 → 方案收尾
```

## 阶段一：拆分与匹配机制

1. 跑 `get_source_profile.py` 取源文件列与行数。
2. 读工作区现状：

```bash
# 工作区表清单
hac table list-tables --space-id '<SPACE_ID>'
# 单表字段配置，关联字段的 config 里有指向的 table_id
hac table get-table --table-id '<TABLE_ID>'
# 整个工作区的表关系图数据
hac table +relation-graph --space '<SPACE_ID>'
```

3. 定主键与拆分：按 `references/keys-and-split.md`；用户没说主键时自己判断并推荐。
4. 产出下面两张表，不写推导过程，数据量用脚本实测的去重后条数。

### 一、导入表拆分

| 次序 | 目标表 | 数据量 | 导入说明 |
| -- | -- | -- | -- |
| 1 | 表名 | N 条 | 这一批取源文件的哪些行/列，为什么先导它 |

### 二、匹配机制

按目标表分块，一张表一个小节：先一句写数据主键和推荐理由，再一张关联匹配表，每格都填满。

```markdown
#### 〈目标表名〉

数据主键：字段A｜字段B（推荐理由一句话）

| 关联字段 | 关联表 | 关联表主键 |
| -- | -- | -- |
| 字段名 | 关联表名 | 字段C｜字段D |
```

5. **停下**，请用户确认拆分是否合理、主键是否正确。确认门只问这两件事，加上主键判定过程中出现的业务岔路（如关联表主键重复的两条路）；列映射、必填、选项映射等配置层问题属于阶段二评估，此处不问。确认前不做任何评估或写入。

## 阶段二：评估、方案、执行、交付

1. 三层评估：按 `references/assessment.md` 逐层做，发现的问题不当场修，全部记进方案 1.2「待确认内容」。产出：源表层重复明细 Excel（冲突行标黄）、配置层逐条记录、数据层三档计数与待导入/疑似匹配 Excel。
2. 匹配率数字先给用户看。
3. 出《导入方案》首版：填 `assets/import-plan-template.md` 两章，2.1 导入总览标"待确认后填定"，连同待导入/疑似匹配 Excel 一起交出待确认。
4. 确认结果回来后重跑数据层评估，匹配率达标才填定 2.1 导入总览；没复检的文件不进总览也不导。
5. 按方案执行：编排细节读 `references/execution.md`，顺序是系统改动与补索引 → 补关联数据 → 采集基线 → 小样本 → 分批导入 → 复扫本批新建的关联表记录，每步回读记进 2.4 执行次序。补关联数据时带全能映射的列，不只导主键。
6. 改动范围只限本批导入建的数据（分几天导的算一批），其他人建的记录只列不动。
7. 方案是唯一事实源：任何变动都当场同步进方案文档，包括确认结论落定（勾销对应条目）、口径或拆分调整（改对应小节）、执行进展（打勾、记回读）、新发现的问题（追加回 1.2）。任何时点打开方案，读到的都是当前最新状态，不存在只在对话里说了、方案里没有的决定。1.2 清空且全量核对通过，文档状态改"已收尾"。
